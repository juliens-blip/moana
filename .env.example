# Never commit real values. Generate unique secrets per environment.
# Example: node -e "console.log(require('crypto').randomBytes(32).toString('base64url'))"

NEXT_PUBLIC_SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
MOANA_SESSION_SECRET=
YATCO_WEBHOOK_SECRET=

# Optional operational scripts; keep source files outside this repository.
BROKER_IMPORT_CSV=
BROKER_IMPORT_JSON=
BROKER_EXPORT_JSON=
BROKER_BOOTSTRAP_PASSWORD=
TEST_BROKER_NAME=
TEST_BROKER_PASSWORD=
