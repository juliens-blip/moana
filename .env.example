# Placeholders only — never put real values here. Real secrets live in
# moana/.env.local / moana/.env, both git-ignored (see .gitignore).

GCP_PROJECT_ID=
GCP_LOCATION=us-central1
GOOGLE_APPLICATION_CREDENTIALS=
VEO_MODEL=veo-3.1-fast-generate-001
VEO_DEFAULT_DURATION_SECONDS=6
VEO_PROVIDER=vertex
