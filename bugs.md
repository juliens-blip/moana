# Bugs

Format : `[YYYY-MM-DD] | Problème | Cause | Solution | #tags`

[2025-12-09] | Le filtre par nom de broker renvoyait une erreur 500 | Un nom était comparé à une colonne UUID | Résoudre le nom en UUID avant filtre, création ou mise à jour | #backend #database #bug #fix #done

[2026-01-06] | L’upload mobile ne déclenchait pas toujours le sélecteur ou la caméra | L’input fichier était masqué de façon inaccessible et mal réinitialisé | Utiliser un input accessible, valider le fichier et réinitialiser après succès | #frontend #bug #fix #done

[2026-01-29] | La création automatisée des brokers JMO et Marc a échoué | L’appel Supabase du script a retourné `fetch failed` | Rejouer le script idempotent avec connectivité et variables vérifiées | #backend #database #bug #blocked

[2026-07-15] | Les mots de passe brokers sont comparés et journalisés en clair | La migration a conservé une authentification provisoire sans hachage | Migrer vers bcrypt et supprimer toute journalisation de secrets | #backend #database #bug #todo

[2026-07-15] | Le cookie de session peut être forgé côté client | La session est un JSON non signé stocké directement dans un cookie | Utiliser une session serveur ou un jeton signé et valider l’identité à chaque requête | #backend #bug #todo

[2026-07-15] | Les routes `/api/debug/env` et `/api/debug/auth` exposent des informations sensibles | Des endpoints de diagnostic non protégés sont restés dans l’application | Les supprimer ou les restreindre strictement hors production | #backend #api #bug #todo

[2026-07-15] | Le KYC Vercel retournait une panne fournisseur et zéro source | DuckDuckGo servait une page anti-bot et Mojeek ne permet pas ce scraping automatisé | Prioriser Wikipedia OpenSearch et Google News RSS, filtrer le terme exact et sérialiser les requêtes | #backend #ai #bug #fix #done
[2026-07-16] | Le bouton KYC retournait HTTP 500 après le déploiement Crawl4AI | `VERCEL_URL` ciblait une URL de déploiement protégée qui renvoyait 401 avant la fonction Python | Appeler prioritairement l’alias Production et utiliser un jeton interne dans `X-Moana-KYC-Token` | #bug #fix #backend #infra #done

[2026-08-13] | Le serveur MCP `qmd` ne démarrait pas sous Linux | La commande utilisait la syntaxe Windows `cmd /c qmd mcp`, invalide sur ce poste | Appeler directement le binaire `qmd` dans `.mcp.json` | #infra #mcp #bug #fix #done

[2026-08-14] | Le build Docker des conteneurs `yatco-global` (collecteur + worker) échouait | `.dockerignore` racine (whitelist stricte) n'autorisait ni `workers/` ni `scripts/yatco_collector.py` | Négations `!workers/`, `!scripts/yatco_collector.py` ajoutées au `.dockerignore` ; les deux `docker build` validés verts le 2026-08-16 | #backend #infra #docker #bug #fix #done

[2026-08-17] | Le mode `check`/préflight de `deploy.py` (déploiement `yatco-global`) restait bloqué contre l'hôte distant | La variable d'environnement `MOANA_SSH_KEY` était absente du contexte local de session ; elle a été fournie et validée (2026-08-18) | `MOANA_SSH_KEY` fourni en environnement, préflight validé, déploiement complété et services vérifiés sur ubuntu@51.44.220.145 | #backend #infra #deploy #bug #fix #done

[2026-08-19] | `moana-yatco-ingest.service` échouait en production avec `ConfigurationError: Missing configuration: NEXT_PUBLIC_SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY` | Le `.env` Docker Compose n'était jamais déployé vers l'hôte distant | `deploy.py` étendu (`load_env_value`/`write_env_file`) pour lire ces variables depuis `os.environ` et transférer un `.env` temporaire 0600 vers `workers/docker/.env` sans les journaliser ; confirmé en production le 2026-08-20 (`production_yatco_ingest_verification.json` : aucune erreur de configuration dans le journal) | #backend #infra #deploy #bug #fix #done

[2026-08-19] | Le health-check `deploy.py` déclarait `moana-yatco-ingest.service` (`Type=oneshot`) en échec malgré un run réussi | Sonde `ActiveEnterTimestamp`/`MainPID`, remis à vide dès qu'un oneshot termine, même en succès | `trigger_and_verify_unit` réécrit pour juger sur `InvocationID`/`ExecMainStartTimestamp` après `reset-failed`+`start` ; confirmé en production le 2026-08-20 (`production_yatco_ingest_verification.json` : exit codes 0, `Result=success`, `files=694 written=4435 dead_letters=2`) | #backend #infra #deploy #bug #fix #done

[2026-08-20] | Suite de tests backend désynchronisée après la réécriture du health-check `deploy.py` | Les mocks `systemctl show` de `test_deploy_env.py`/`test_deploy_service_trigger.py` simulaient encore les anciennes propriétés `ActiveEnterTimestamp`/`MainPID` au lieu de `InvocationID`/`ExecMainStartTimestamp` | Mocks réécrits sur les nouvelles propriétés ; confirmé le 2026-08-27 (`.venv/bin/python3 -m pytest tests/backend -q` → 298 passed/7 skipped/0 failed, `test_deploy_env.py`/`test_deploy_service_trigger.py` 18/18) | #backend #test #bug #fix #done

[2026-08-26→2026-08-27, résolu en 2 temps] | Routes brochure-video `/api/brochure-video` en prod : d'abord 500 « Configuration serveur indisponible », puis 502 « Échec du lancement du job distant » une fois les env vars ajoutées | (1) `MOANA_SSH_KEY`/`MOANA_SSH_HOST` absents de Vercel Production → ajoutés par l'utilisateur. (2) Logs runtime Vercel : `spawn ssh ENOENT` — le binaire système `ssh` n'existe pas dans le runtime serverless Node.js/AWS Lambda de Vercel, `lib/brochure-video-upload.ts` faisait `spawn('ssh', ...)` | `createSshRunner` réécrit avec la lib `ssh2` (client SSH pur Node/JS, sans dépendance à un binaire système) ; `ssh2` déclaré en `experimental.serverComponentsExternalPackages` dans `next.config.js` (son binding natif crypto casse sinon le bundling webpack) ; `ssh2`+`@types/ssh2` ajoutés à `package.json` ; tests 36/36 verts, `tsc --noEmit` propre, `npm run build` réussi | #frontend #backend #deploy #bug #fix #done

[2026-08-26, résolu et vérifié en production le 2026-08-27] | Service systemd `moana-brochure-video@.service` manquant sur EC2 ubuntu@51.44.220.145 | `REMOTE_COMMANDS` de `deploy.py` ne copiait jamais `moana-brochure-video@.service` vers `/etc/systemd/system/` (seules les 3 unités YATCO l'étaient) ; `sudo systemctl start moana-brochure-video@<jobId>` échouait donc côté API | `workers/deploy/deploy.py` corrigé (ligne `sudo cp workers/deploy/moana-brochure-video@.service /etc/systemd/system/...` ajoutée avant `daemon-reload`) ; `deploy.py --apply` exécuté par l'utilisateur (`echo $?` → 0, unité confirmée présente via `ls -la /etc/systemd/system/moana-brochure-video@.service` sur l'EC2) | #backend #infra #deploy #bug #fix #done

[2026-08-27, résolu en 2 temps] | Après le fix ssh2 ci-dessus, `/api/brochure-video` en prod échouait encore : d'abord `Cannot parse privateKey: Unsupported key format`, puis `connect ETIMEDOUT 51.44.220.145:22` | (1) La valeur Vercel de `MOANA_SSH_KEY` incluait encore le préfixe shell `MOANA_SSH_KEY="..."` copié depuis le `.env` source au lieu du seul contenu PEM — corrigé par l'utilisateur dans Vercel. (2) Security Group AWS de l'EC2 restreignait le port 22 à une IP fixe ; les fonctions Vercel appellent depuis des IPs AWS dynamiques non whitelistées | Valeur `MOANA_SSH_KEY` nettoyée dans Vercel (uniquement le bloc PEM, sans guillemets ni préfixe) ; règle inbound TCP 22 du Security Group EC2 élargie à `0.0.0.0/0` par l'utilisateur (authentification restant par clé uniquement, jamais par mot de passe) | #frontend #backend #infra #deploy #bug #fix #done

[2026-08-27→2026-08-28] | Job brochure-video lancé avec succès (SSH + systemd OK) mais échouait ensuite avec `PdfExtractionError: object 28 has a non-literal /Length` sur un PDF réel utilisateur | `workers/pdf_image_extractor.py` est un parseur PDF maison sans dépendance tierce (volontaire, cf. docstring du fichier) qui exigeait un `/Length` entier littéral ; le PDF de l'utilisateur a un objet dont `/Length` est une référence indirecte (`N G R`), valide selon la norme PDF mais jamais résolue par ce parseur à passe unique | Repli sur la recherche du marqueur littéral `endstream` (même stratégie que les lecteurs PDF tolérants) quand `/Length` n'est pas un entier littéral, au lieu de lever une erreur ; test de régression ajouté (`test_indirect_stream_length_falls_back_to_endstream_marker`) ; 299/299 tests backend verts, `ruff check` propre | #backend #bug #fix #done

[2026-08-20] | Le dashboard YATCO Global (fenêtre 72h) renvoyait 0 résultat malgré 4438 annonces en base | La vue `yatco_selection_candidates` (migration `20260814T1930`) filtrait sur `source_created_at`/`source_updated_at`, des dates publiées par YATCO (jamais remplie / quasi figée), pas nos dates d'ingestion | Migration `20260820T1615__fix_yatco_selection_freshness_signal` bascule le filtre et le tri sur `first_seen_at`/`updated_at` ; `lib/supabase/yatco-global.ts` aligné ; vérifié en production, 780 lignes désormais retournées | #backend #database #bug #fix #done

[2026-08-21] | Les filtres YATCO Global restaurés depuis l'URL rejetaient une actualisation dès que `length_m=0` était présent | `0` était envoyé tel quel au schéma de validation strict, qui le traite comme une valeur invalide plutôt qu'une absence de filtre | `0` traité comme absence de filtre et omis de la requête API ; 25 tests frontend passent | #frontend #validation #bug #fix #done

[2026-08-22] | Le flux YATCO Global affichait 0 annonce alors que des annonces existaient côté BOSS | Le cookie de session `BOSSAuthCookie` avait expiré : le cron de réauthentification (27h) dépassait la durée de vie réelle du cookie, et une page de login renvoyée par le pager était silencieusement traitée comme une liste vide | Intervalle de cron réduit à 20h ; le scraper détecte désormais explicitement une page de login au lieu de la convertir en liste vide | #infra #auth #bug #fix #done

[2026-08-22] | Le téléchargement de brochure YATCO renvoyait une erreur 404 ou restait bloqué en chargement | Le bouton appelait directement `/forsale/pdf/custompdf/`, une page de configuration broker qui ne fait pas partie du parcours fonctionnel de l'interface | Remplacé par le workflow authentifié `quickpdf` → attente filestore → URL S3 (`scripts/yatco-boss-brochure.mjs`, `/api/yatco-global/brochure`) ; validé avec un PDF réel de 6,86 Mo | #backend #api #bug #fix #done

[2026-08-26, corrigé côté code au 2026-08-27, déploiement EC2 restant] | Session Software Factory `20260825T215927-438096` : brochure-video pipeline T1 builder généra l'implémentation, mais T1 tester rejeta le déploiement car template systemd crée mais jamais copié vers /etc/systemd/system | DEPLOY_ARTIFACTS et REMOTE_COMMANDS mal alignés — plan déploiement envoie le template à ~/moana sans que la step de finalisation systemd ne le copie/installe en prod | `workers/deploy/deploy.py` : `REMOTE_COMMANDS` complété avec le `cp .../moana-brochure-video@.service /etc/systemd/system/` manquant, avant `daemon-reload` ; non commité, non encore appliqué sur l'EC2 (`deploy.py --apply` à exécuter) | #backend #infra #deploy #bug #in-progress

[2026-08-22] | Le layout du dashboard affichait un filigrane logo Moana en très grand, particulièrement visible sur YATCO Global | Une image JPEG 1600x800 était appliquée en `background-repeat` sur toute la zone dashboard, rendu envahissant et dépendant du cache | Filigrane retiré de `app/dashboard/layout.tsx` ; le logo reste uniquement dans le header | #frontend #ui #bug #fix #done

[2026-08-27] | `tests/frontend/yatco-stats.test.ts` échoue sur « missing fetch call to /api/yatco-stats » (19/20) alors que `components/dashboard/YatcoStatsCard.tsx` appelle bien l'endpoint | L'assertion (ligne 210) cherche la regex `fetch('/api/yatco-stats')` sans second argument, mais le composant appelle correctement `fetch('/api/yatco-stats', { cache: 'no-store' })` | Corriger la regex du test pour accepter un second argument optionnel ; l'app n'a pas de bug | #frontend #test #bug #todo

[2026-08-30] | `AttributeError: 'ExtractedImage' object has no attribute 'mime_type'` à la génération de brochure-video | Le champ `mime_type` avait été développé (PNG pour Flate+SMask, JPEG réel pour DCTDecode/JPXDecode) mais restait uniquement dans un `git stash` sur `fix/yatco-global-vercel`, jamais appliqué sur `main`, alors que les consommateurs (`gemini_pdf_classifier.py`, `brochure_video_runner.py`, `vertex_veo_transport.py`) lisaient déjà ce champ | Diff isolé du stash réappliqué sur `main` (`workers/pdf_image_extractor.py` + tests dédiés) sans le reste du stash ; 30/31 tests `test_pdf_image_extractor.py` (1 échec préexistant sans rapport) + smoke test brochure-video vert | #backend #bug #fix #done

[2026-08-30] | Génération de brochure-video bloquée ~10 min côté UI sans erreur affichée | `ffmpeg` tué par OOM (`oom-kill`) pendant l'assemblage des clips avec transitions `xfade` sur l'EC2 dédié `moana-brochure-video` (1,9 Go RAM, 0 swap) ; SIGKILL empêche l'écriture d'un statut d'erreur, d'où le blocage silencieux | Mitigation : 4 Go de swap ajoutés (`/swapfile`, persistant, `vm.swappiness=10`) + `systemctl reset-failed` sur les unités bloquées ; pas de resize d'instance ni de changement de code. Résiduel : un job tué par OOM ne remonte toujours pas de statut d'échec exploitable côté UI | #backend #infra #bug #fix #done

[2026-08-30] | `GET /api/yatco-global/brochure` renvoyait 502 en production alors que le script distant/EC2/session BOSS fonctionnaient hors Vercel | Un second EC2 (`moana-brochure-video`) créé le 2026-08-29 a fait repointer les variables Vercel partagées `MOANA_SSH_KEY`/`MOANA_SSH_HOST` vers ce nouveau serveur, cassant le fallback de la route brochure BOSS faute de `YATCO_SSH_KEY`/`YATCO_SSH_HOST` dédiées (prévues dans le code, jamais renseignées côté Vercel) | `YATCO_SSH_KEY` (clé `moanakyc.pem`) et `YATCO_SSH_HOST` (`ubuntu@51.44.220.145`) ajoutées en Production Vercel, puis redéploiement ; aucun changement de code ; confirmé fonctionnel par l'utilisateur | #backend #infra #deploy #bug #fix #done

[2026-09-03] | `BrochureEditorialDirectionError: gemini brochure direction unavailable: ClassificationTransientError: gemini brochure director network_error:TimeoutError` faisait échouer tout le job brochure-video dès le premier aléa réseau | L'appel Gemini "brochure director" (PDF entier + toutes les images en un seul appel, `workers/brochure_video_runner.py`) était configuré avec `max_retries=0`, contrairement à tous les autres appels Gemini du pipeline (3 retries par défaut) ; un simple `TimeoutError` transitoire tuait donc le job entier sans retry, malgré un budget job de 3600s largement suffisant | `director_settings` passé à `max_retries=2` (`workers/brochure_video_runner.py:935`, même config répliquée dans `tests/backend/test_remote_brochure_video_smoke.py`) ; test de régression ajouté (`test_director_retries_a_transient_network_timeout_then_succeeds`, `tests/backend/test_brochure_editorial_direction.py`) ; 2 unités systemd `moana-brochure-video@*` restées en échec depuis les tentatives précédentes nettoyées (`systemctl reset-failed`) ; suite backend 368/369 verte (1 échec préexistant sans rapport, `SUGGESTED_SECTION_CATEGORIES`) ; déployé en production (`deploy_brochure_video.py --apply`, backup `brochure-video-20260903T135828Z-9d0744f4`), fix vérifié présent sur le remote | #backend #bug #fix #done

[2026-09-03, suite du bug ci-dessus] | Même job échoue à nouveau juste après le fix `max_retries=2` ci-dessus, cette fois `... gemini brochure director http_503` | `journalctl` sur l'EC2 confirme le fix précédent était bien actif (job démarré après le déploiement) : 3 tentatives sur ~250s, toutes en `http_503` — le backoff total (~3s sur 2 retries, `backoff_base_s`/`backoff_cap_s` par défaut 1s/10s) est bien trop court face à une surcharge Gemini qui se résorbe en dizaines de secondes, pas en 1-3s | `director_settings` : `max_retries=2→4`, `backoff_base_s=5.0`, `backoff_cap_s=45.0` (`workers/brochure_video_runner.py:938`, répliqué dans le smoke test) ; log de retry ajouté (`make_gemini_brochure_director` n'avait pas de `on_retry`, contrairement au classifieur image-par-image — invisible en cas de nouvel échec) ; suite backend 368/369 verte (même échec préexistant sans rapport) ; déployé en production (backup `brochure-video-20260903T164233Z-e35d9ff9`), fix vérifié présent sur le remote | #backend #bug #fix #done
